{
  "title": "Pandas Cheatsheet",
  "language": "en",
  "tags": ["pandas", "python", "data-science", "library"],
  "items": [
    {
      "code": "pd.DataFrame()",
      "description": "Create a DataFrame",
      "example": "import pandas as pd\ndf = pd.DataFrame({'col1': [1, 2], 'col2': [3, 4]})"
    },
    {
      "code": "pd.read_csv()",
      "description": "Read data from a CSV file",
      "example": "df = pd.read_csv('data.csv')"
    },
    {
      "code": "df.head(), df.tail()",
      "description": "View the first/last rows",
      "example": "print(df.head(5))"
    },
    {
      "code": "df.describe()",
      "description": "Statistical summary of the data",
      "example": "print(df.describe())"
    },
    {
      "code": "df['column']",
      "description": "Select a column",
      "example": "ages = df['age']"
    },
    {
      "code": "df.loc[], df.iloc[]",
      "description": "Select rows/columns by label or position",
      "example": "df.loc[0, 'col1']  # By label\ndf.iloc[0, 0] # By position"
    },
    {
      "code": "df.groupby()",
      "description": "Group data",
      "example": "df.groupby('country').mean()"
    },
    {
      "code": "pd.merge()",
      "description": "Merge DataFrames",
      "example": "pd.merge(df1, df2, on='id')"
    }
  ],
  "created": "2025-09-26",
  "updated": "2025-10-01"
}
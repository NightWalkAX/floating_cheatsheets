{
  "title": "Pandas Cheatsheet",
  "language": "en",
  "cheats": [
    {
      "description": "Create DataFrame",
      "code": "pd.DataFrame({'a': [1,2]})",
      "example": "# Pandas operation"
    },
    {
      "description": "Read CSV file",
      "code": "pd.read_csv('file.csv')",
      "example": "# Returns: DataFrame with CSV data"
    },
    {
      "description": "Select column",
      "code": "df['col']",
      "example": "# Usage example for: df['col']..."
    },
    {
      "description": "Filter rows",
      "code": "df[df['col'] > 0]",
      "example": "# Usage example for: df[df['col'] > 0]..."
    },
    {
      "description": "Group by column",
      "code": "df.groupby('col').sum()",
      "example": "# Usage example for: df.groupby('col').sum()..."
    },
    {
      "description": "Sort by column",
      "code": "df.sort_values('col')",
      "example": "# Usage example for: df.sort_values('col')..."
    },
    {
      "description": "Describe DataFrame",
      "code": "df.describe()",
      "example": "# Usage example for: df.describe()..."
    },
    {
      "description": "Drop column",
      "code": "df.drop('col', axis=1)",
      "example": "# Usage example for: df.drop('col', axis=1)..."
    }
  ]
}
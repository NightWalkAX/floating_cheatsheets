{
  "title": "Scanner API",
  "language": "en",
  "tags": ["javascript", "api", "frappe", "scanner_api"],
  "cheats": [
    { "description": "Create a scanner instance", "code": "new frappe.ui.Scanner({ container, dialog, multiple, on_scan })" },
    { "description": "Scan a single barcode", "code": "new frappe.ui.Scanner({ dialog: true, multiple: false, on_scan(data) { console.log(data.decodedText); } })" },
    { "description": "Scan multiple barcodes", "code": "const scanner = new frappe.ui.Scanner({ dialog: true, multiple: true, on_scan(data) { handle_scanned_barcode(data.decodedText); } })" },
    { "description": "Stop scanning", "code": "scanner.stop_scan();" }
  ]
}
- (COMPLETADO) ✅ Implementar buscador en el menu contextual (para que en el menu dial se muestren la(s) cheatsheets ) y menu de gestion de CheatSheets
  * ✅ Creado módulo search_components.py con SearchDialog y QuickSearchEntry
  * ✅ Integrado buscador en menú contextual del widget principal
  * ✅ Añadida funcionalidad de búsqueda rápida en TagManager (pestaña cheatsheets)
  * ✅ Búsqueda avanzada con filtros por tags y lenguajes
  * ✅ Actualizado DialMenu para incluir opción de búsqueda en menú contextual central

- Arreglar el install options en index.html

- (MAS ADELANTE) Exportar/Importar paquetes de cheatsheets para poder compartirlos y extraerlos con comodidad

- Arreglar la importacion en la instalacion de el archivo de idiomas
- Arreglar el problema de los scripts de desinstalacion:
aparece en el menu de aplicaciones, y cuando lo toco funciona y corre

sudo rm -rf /usr/bin/floating-cheatsheets

sudo rm -rf /usr/share/pixmaps/floating-cheatsheets.png

sudo rm -rf /usr/share/applications/floating-cheatsheets.desktop


sudo rm -rf /usr/share/floating-cheatsheets

sudo rm -rf /usr/share/doc/floating-cheatsheets

sudo update-desktop-database

sudo gtk-update-icon-cache /usr/share/pixmaps/ 2>/dev/null || echo "Cache actualizado"

sudo dpkg --forget-old-unavail
- Arreglar el problema de actualizacion, ya que al actualizar hay problemas de que mantiene el codigo anterior.